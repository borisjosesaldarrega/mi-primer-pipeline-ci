---
extends: default

rules:
  document-start: disable
  line-length:
    max: 120
  truthy:
    allowed-values:
      - "true"
      - "false"
      - "on"
    check-keys: false

ignore: |
  node_modules/
